// This program adds two integers and prints out the result.

#include <stdio.h>

int main(void)
{
    int a,b,sum;
    a = 50;
    b = 25;
    sum = a + b;

    printf("The sum of %i and %i is %i.\n", a,b,sum);
    return 0;
}
